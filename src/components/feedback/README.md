# Feedback Components

Shared loading, toast, empty, and error states live here. Keep them presentation-only.

Use `Toast` for user-triggered success, warning, and failure feedback. Pair it with `ToolbarButton`'s loading state for save/update actions.
