# Settings

Local settings module for costing defaults, HueForge thresholds, currency display, unit display, and dark-mode preference.

Settings are stored locally through `src/data/settings/localSettingsRepository.ts` and validated in `src/domain/settings`. This feature does not add authentication, cloud sync, remote accounts, or Firebase configuration.
