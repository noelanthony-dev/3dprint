import { createScaffoldModuleStatus } from "@/domain/shared";

export const pricingDomainStatus = createScaffoldModuleStatus({
  layer: "domain",
  name: "pricing",
  notes: ["Future sale unit and price guidance rules."],
});

