{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop permissions for the scaffolded offline app.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
