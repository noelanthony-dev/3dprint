import { createScaffoldModuleStatus } from "@/domain/shared";

export const hueforgeDomainStatus = createScaffoldModuleStatus({
  layer: "domain",
  name: "hueforge",
  notes: ["Future filament color, TD, and stock matching logic."],
});

