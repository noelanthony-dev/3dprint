import { createScaffoldModuleStatus } from "@/domain/shared";

export const costingDomainStatus = createScaffoldModuleStatus({
  layer: "domain",
  name: "costing",
  notes: ["Future pure print profile and cost calculations."],
});

